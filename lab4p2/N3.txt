#include <iostream>
using namespace std;
void func(int a,int b);

int main()
{
    setlocale(0, "RUS");
    cout << "Введите m: ";
    float m;
    cin >> m;
    cout << "Введите n: ";
    float n;
    cin >> n;
    cout << "A(m, n)= ";
    func(m, n);
   
}
void func(int a, int b)
{
    if (a == 0) return func(a, b + 1);

    else if (a > 0 && b==0) return func(a - 1, 1);

    else return func(a - 1, func(a, b - 1));
}